class Person {
  init(name, age) {
    this.name = name;
    this.age = age;
  }

  show() {
    print "[NAME]: " + this.name + " [AGE]: " + this.age;
  }
}

let Daniel = Person(
  "Daniel",
  "14"
);
Daniel.show();
