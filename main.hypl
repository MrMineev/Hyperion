fun fibo(n) {
  if (n < 2) return n;
  return fibo(n - 2) + fibo(n - 1);
}

let start = clock();
for (let i = 0; i < 35; inc i) {
  let random;
  print random = fibo(i);
}
let end = clock();

print end - start;

